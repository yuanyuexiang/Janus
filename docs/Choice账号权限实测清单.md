# Choice 账号权限实测清单

> 提交给东方财富客户经理用于确认权限范围

**账号**：njnbt0001
**测试时间**：2026-06-03
**SDK 版本**：EmQuantAPI Python V2.6.1.3
**登录方式**：账密 `ForceLogin=1,UserName=njnbt0001,Password=...`（成功）
**测试环境**：macOS（Apple Silicon，Rosetta 转译运行 x86_64 SDK）

所有结论均基于在该账号上的真实 SDK 调用结果，错误码为 Choice 服务端返回的原始 code。

---

## ✅ 已开通 · 可用

### 登录 / 鉴权

| 项 | 状态 | 说明 |
|---|---|---|
| 账密直登 `ForceLogin=1` | ✅ | 服务端日志：`user pwd login start success!` |
| SDK 二进制加载 / ServerList 下载 | ✅ | `bjse_code_conversion.txt` 自动更新成功 |

### 截面数据 `css`（个股静态字段）

| 字段 | 含义 | 状态 | 实测样本（600519.SH 茅台） |
|---|---|---|---|
| `NAME` | 证券名称 | ✅ | "贵州茅台" |
| `PETTM` | TTM 市盈率 | ✅ | 19.37 |
| `PBMRQ` | 市净率（最新报告期）| ✅ | 5.92 |
| `VOLUME` | 成交量（历史日） | ✅ | 5,247,652 |
| `AMOUNT` | 成交额（历史日） | ✅ | 67.43 亿 |

### 历史行情 `csd`（日线 K 线）

| 字段 | 状态 |
|---|---|
| `OPEN / HIGH / LOW / CLOSE` | ✅ 完整可用 |
| `VOLUME / AMOUNT` | ✅ 完整可用 |
| 复权参数 `AdjustFlag=1` | ✅ |
| 日期范围（试到 ~30 天）| ✅ 含 2026-06-03 收盘 |

### 宏观数据 `edb`

| 测试指标 | EDB ID | 状态 | 实测值 |
|---|---|---|---|
| CPI 同比 | EMM00087117 | ✅ | 8.5% (2025/12) |
| PPI / M2 / 制造业 PMI / 10Y 国债 / 社融 / USD-CNY | (推断) | 🟡 待验 | 同属 EDB 体系，预期可用 |

### 指数行情（基于代码空间测试）

| 代码空间 | 示例 | 状态 | 实测值 |
|---|---|---|---|
| 中证指数 `.CSI` | `930707.CSI` 中证畜牧养殖 | ✅ | 1862.85 |
| 国证指数 `.SZ` | `399997.SZ` 中证白酒 | ✅ | 6937.71 |
| 国证指数 `.SZ` | `399396.SZ` 国证食品饮料 | ✅ | 15066.29 |
| 上证行业 `.SH` | `000932.SH` 中证主要消费 | ✅ | 12856.33 |

---

## ❌ 未开通 / 受限 · 希望确认开通

### 1. 实时行情字段（最迫切需要）

| 字段 | 含义 | 错误码 | 当前替代方案 |
|---|---|---|---|
| `NOW` | 最新成交价 | **10000013 service error** | 用 `csd` CLOSE 最新一根（T-1 收盘价） |
| `CHGPCT` | 当日涨跌幅 | **10000013 service error** | 用 csd CLOSE 自算 `(last-prev)/prev` |
| `PCTCHG` | 日涨跌幅（历史） | **10000013 service error** | 同上 |
| `PCHG` | 涨跌价格 | **10000013 service error** | 同上 |
| `CHGPCTNYEAR` | 年初至今涨幅 | **10000013 service error** | 用 csd 年初首根 vs 最新自算 YTD |

**📌 问题影响**：
投研场景虽可用 T-1 收盘价绕过，但接口"最自然"的 `css` 实时调用全部不通。
希望确认 njnbt0001 是否可以开通 **实时行情字段**（`NOW` / `CHGPCT` / `PCTCHG` / `CHGPCTNYEAR`）。

### 2. 申万行业指数

| 代码空间 | 测试样本 | 错误码 |
|---|---|---|
| 申万指数 `.SI` | `801124.SI` 白酒、`801010.SI` 农林牧渔、`801120.SI` 食品饮料 | **10003008 invalid stock code** |

**📌 问题影响**：
A 股研究最常用的"申万一/二级行业分类"全部不可访问。
当前已绕用中证/国证指数，但行业归类口径不一致。
希望确认能否开通 **申万指数体系**（`801xxx.SI` 全系列）。

### 3. 其他指数空间（次优先级）

| 代码空间 | 测试样本 | 错误码 |
|---|---|---|
| 万得行业 `.WI` | `CI005019.WI` 万得白酒 | **10003008 invalid stock code** |
| 东财板块 `.BK` | `BK0438.BK` 东财白酒板块 | **10003008 invalid stock code** |

**📌 影响**：替代行业体系。单一开通申万即可满足需求。

### 4. 资讯接口 `cfn` ⚠️ 完全无权限

| 资讯类型 | 错误码 | 服务端日志 |
|---|---|---|
| `companynews` 公司新闻 | **10001012 insufficient user access** | `returnCode:10001, returnMsg:无权限[10001]` |
| `industrynews` 行业新闻 | (同) | (同) |
| `sectornews` 板块新闻 | (同) | (同) |
| `report` 研报 | (同) | (同) |
| `regularreport` 定期报告 | (同) | (同) |
| `tradeinfo` 交易信息 | (同) | (同) |

**📌 问题影响**：
整个新闻 / 研报 / 公告体系不可访问。
我们的投研产品中"风险派"和"宏观派"AI 顾问需要新闻面信息，目前已降级到第三方数据源。
希望确认能否开通 **资讯查询接口 `cfn`**（`companynews` 或 `report` 任一为优先）。

---

## ❓ 待客户经理确认的额外问题

### 问题 1：短信激活 SXDL 模式是否仍可用？

账号开通量化权限后我们尝试 `c.start("LoginMode=SXDL,PhoneNumber=15077826106")` 报错：

```
code=10001026, msg='invalid mobile original'
```

手机号 15077826106 是账号绑定用户本人。
**疑问**：权限开通后此模式是否自动被禁用，必须走账密直登？

### 问题 2：会话并发限制

账密 `ForceLogin=1` 模式下，同账号能否多进程 / 多线程并行调用？
我们目前用 RLock 在 gateway 层做了串行化保护，希望确认实际并发上限。

### 问题 3：PCTCHG 字段权限归属

- `CHGPCT`（截面实时涨跌）在 `css` 调用返 10000013
- `PCTCHG`（历史涨跌）在 `css` 调用也返 10000013
- `PCTCHG` 在 `csd` 历史日线调用接口通但全部返 `None`

**疑问**：这三种调用是同一组权限？还是分别属于不同套餐？

---

## 优先级建议

| 优先级 | 权限 | 影响 |
|---|---|---|
| 🔴 高 | 实时行情 `NOW / CHGPCT / CHGPCTNYEAR` | 解锁最自然的截面调用，去掉 backend 层的 T-1 绕路 |
| 🔴 高 | 申万指数 `801xxx.SI` 全系列 | 投研标准行业分类，避免代码空间割裂 |
| 🟡 中 | 资讯接口 `cfn` | 解锁新闻 / 研报 / 公告维度 |
| 🟢 低 | 万得 `.WI` / 东财 `.BK` | 申万开通后非必需 |

---

## 附：当前可用接口的实际工作流

为说明账号当前能力到底覆盖了多少投研场景，附上 4 个已跑通的端到端示例：

1. **行情快照**（绕路：`css` 静态 + `csd` 最新一根自算涨跌）
   ```
   贵州茅台 1281.91 元 / -1.94% / PE 19.37 / PB 5.92 / 2026-06-03
   ```

2. **日 K 线**（直接：`csd`）
   ```
   600519.SH 最近 5 日完整 OHLCV
   ```

3. **宏观指标**（直接：`edb`）
   ```
   CPI 同比 8.5%（2025/12）
   ```

4. **行业指数**（绕路：用中证代码替代申万，`csd` 算 YTD）
   ```
   中证白酒指数 6937.71 / 日涨 -1.16% / 年初至今 -22.38%
   ```

资讯类需求当前完全降级到第三方数据源。
